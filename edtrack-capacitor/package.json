{
  "name": "edtrack-capacitor",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "start": "npx cap run android",
    "build": "npx cap sync && cd android && gradlew.bat assembleDebug",
    "build:release": "npx cap sync && cd android && gradlew.bat assembleRelease",
    "keystore": "powershell -ExecutionPolicy Bypass -File scripts/generate-keystore.ps1",
    "apk:debug": "npx cap sync && cd android && gradlew.bat assembleDebug",
    "apk:release": "powershell -ExecutionPolicy Bypass -File scripts/generate-keystore.ps1 && npx cap sync && cd android && gradlew.bat assembleRelease",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "eductrack",
    "mobile",
    "capacitor",
    "android"
  ],
  "author": "Eductrack Team",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/geolocation": "^8.2.0",
    "@capacitor/ios": "^8.1.0",
    "@capacitor/push-notifications": "^8.1.1",
    "@capacitor/share": "^8.0.1"
  },
  "description": "Application mobile Eductrack - Suivi pédagogique"
}
